#include <bits/stdc++.h>

/*
struct node{
int data;
struct node *next;
struct node *prev;
}
typedef struct nnode node;

node *ptr = (node*)malloc (sizeof (node));

todo -> all operation in doubly linked_list
todo -> jospah problem using cirular linked_list

file todo
all operation of singly, doubly, circular linked_list
and jospah problem,
reverse linked_list=> using 3 pointer ,
merging two linked_list,
*/

int arr_circular(int n, ){
  
}
int main(int argc, char const *argv[]) {
  /* code */
  return 0;
}
